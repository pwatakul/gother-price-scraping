---
title: "REQ-002: Price History & Automated Scraping"
type: requirement
version: "1.3"
date: 2026-08-17
status: Active
tags: [requirement, history, analytics, automation, data]
supersedes: "[[REQ-002-v1.2]]"
related: ["[[REQ-001-v1.5]]", "[[REQ-003-v1.0]]", "[[REQ-005-v1.2]]", "[[REQ-008-v1.1]]", "[[ADR-012-group-search-config]]"]
---

# REQ-002: Price History & Automated Scraping
_Version: 1.3 | Supersedes [[REQ-002-v1.2]]_

## What changed from v1.2
| Change | Reason |
|--------|--------|
| **`search_days_ahead` is now a set of booking windows**, chosen from the scheduler's standard 1/3/7/14/30; one run queues one job per window | A single manual window produced series nobody could compare — real runs created +30/+35, sitting apart from the scheduled grid (see [[ADR-013-booking-window-comparison-basis]]). Picking from the standard set means manual runs reinforce the same windows |
| **Length of stay fixed at 1 night; `search_los_variants` dropped** (migration `024`) | A booking window only means something if the stay it measures is identical across windows. A stored setting that can never vary is the dead-surface pattern removed in migrations 019 and 022 |
| `POST /hotel-groups/:id/search-runs` returns `{jobs_queued, job_ids}` instead of one job | A run is now a set of jobs |
| Scheduler and manual runs share `queue_window_jobs` | They already did the same thing — one job per window at one night — from two copies of the loop |
| Migration `025` corrects the "at least one window" constraint | `array_length('{}', 1)` is NULL, and a CHECK evaluating to NULL passes, so migration 024's constraint accepted an empty selection. `cardinality()` returns 0 and rejects it |

## What changed from v1.1
| Change | Reason |
|--------|--------|
| **New F-012–F-014** — a hotel group carries saved price-search settings that can be edited, run in one click, and are shared with the scheduler | A search was reconfigured from scratch every run; nothing was remembered, so repeating last week's search meant re-entering it from memory |
| Dates stored as a **days-ahead offset**, not calendar dates | A saved config holding literal dates goes stale silently — it keeps querying a past date and just stops producing data. See [[ADR-012-group-search-config]] |
| **`scheduled_scrape_configs.method` dropped** (migration `022`); the scheduler reads `hotel_groups.search_method` | With the group as the source of truth, a per-schedule method is a second place to set the same thing, free to diverge silently — the dead-config problem migration `019` already corrected once |
| Scheduled runs still use the **fixed standard grid** for windows and stay params | The group config supplies the method only. [[ADR-006-booking-window-device-standard]] keeps every hotel's series on one x-axis, which per-group stay params would break |

## Functional Requirements — status
| ID | Description | Status |
|----|-------------|--------|
| F-001 | Dual-write: scrape_results + hotel_price_history | Done — `worker/jobs/scrape_job.rs::save_results` |
| F-002 | hotel_price_history schema | Done — migration `013`, extended by `017` (device) and `021` (via_method) |
| F-003 | Create scheduled scrape config | Done — `POST /scheduled-scrape-configs` |
| F-004 | List/update/delete scheduled scrape configs | Done |
| F-005 | Cron worker fires scrape jobs | Done — `worker/scheduler.rs`, ticks every 60s |
| F-006 | ~~Lookahead scraping from per-config array~~ | Superseded by [[REQ-008-v1.1]]'s fixed booking-window grid |
| F-007 | `GET /price-history` filtered query | Done |
| F-008 | `GET /price-history/hotel/:id/trend` | Done |
| F-009 | Idempotency (no duplicate jobs same config+window same day) | Done — the grid is a constant set |
| F-010 | Configurable retention/cleanup | Deferred — retention resolved to "keep forever" |
| F-011 | Admin manual-trigger cleanup API | Deferred — depends on F-010 |
| **F-012** | **A hotel group stores its own price-search settings: method, a set of booking windows, rooms, adults. Stay length is fixed at 1 night** | **Done, revised in v1.3** — migrations `022`/`024`/`025`, `PUT /hotel-groups/:id/search-config` |
| **F-013** | **The saved search runs in one click, queueing one job per configured window; check-ins derived from the stored offsets at run time** | **Done, revised in v1.3** — `POST /hotel-groups/:id/search-runs` via the shared `queue_window_jobs`; pure `stay_dates()`; "Run Price Search" button |
| **F-014** | **Scheduled runs use the group's configured method, keeping the standard grid for windows and stay parameters** | **Done (new in v1.2)** — `fire_grid` reads `group.search_method` |

## Scope notes
- A schedule now configures only *when* (`cron_expression`) and *whether* (`is_active`) to run. Method comes from the group; windows and stay params are system constants.
- Manual runs set `force_refresh: true` — an explicit press means "get current prices". Scheduled runs leave it false and accept cached rows.
- On-demand jobs via `POST /scrape-jobs` still accept free-form parameters; the saved config does not restrict the API, only the UI's one-click path.

## Verification
Confirmed live:
1. Saved `gemini / 21 days ahead / 2 rooms / 3 adults / LOS [1,3]`; re-read via `GET /hotel-groups/:id` returned the same values.
2. **Run Price Search** produced a job with `checkin_date = today + 21`, `checkout_date = checkin + 3` (longest LOS), `rooms 2`, `adults 3`, `method gemini`, `force_refresh true`.
3. A schedule's **Run now** queued 5 jobs using **gemini** (proving the scheduler reads the group config) while keeping `rooms 1`, `adults 2`, `LOS [1]` and check-ins at +1/+3/+7/+14/+30 — the standard grid, unchanged.
4. All 22 migrations replayed into a throwaway database before the live one; `hotel_groups` gained the five columns with correct defaults and `scheduled_scrape_configs` lost `method`.
5. 55/55 backend tests pass (including `search_window` boundary cases); `tsc --noEmit` clean.

## Open Risks
| Risk | Detail |
|------|--------|
| **Editing the config shifts the manual-search series** | Rooms/adults/LOS are recorded per row so history stays interpretable, but a mid-series config change shows as a step change in the manual-search trend. The scheduled series is unaffected. |
| **One-click runs are easy to repeat** | 20 hotels = 20 SerpAPI searches per press, against a ~250/month free tier. |
| **Two notions of "days ahead"** | `search_days_ahead` (configurable, manual runs) and the booking-window constants (fixed, scheduled runs) are independent by design but easy to conflate when reading the code. |

## Change Log
| Version | Date | Change | Reason |
|---------|------|--------|--------|
| 1.0 | 2026-04-27 | Initial draft | CEO vision requires historical data and automation |
| 1.1 | 2026-08-08 | Implemented F-001–F-009; deferred F-010/F-011 | Implementation pass |
| 1.2 | 2026-08-16 | Added F-012–F-014 (saved per-group search config, one-click run, scheduler reads group method); dropped `scheduled_scrape_configs.method` | Searches were reconfigured from scratch every run, and method was settable in two places that could silently diverge |
| 1.3 | 2026-08-17 | Booking windows became a multi-select from the standard set; stay fixed at 1 night and `search_los_variants` dropped; run returns `{jobs_queued, job_ids}`; shared `queue_window_jobs`; constraint fix in `025` | The config had its two dimensions the wrong way round, and single-window manual runs produced data that could not be compared with scheduled runs |
